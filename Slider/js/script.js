let mySlider = document.querySelector('.my_slider');
mySlider.customSlider({
    // slideWidth: 300,
    // slideHeight: 200,
    // maxSlides: 2,
});
let newSlider = document.querySelector('.new_slider');
newSlider.customSlider({
    slideWidth: 300,
    // slideHeight: 200,
    // maxSlides: 2,
});